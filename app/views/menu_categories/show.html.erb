<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @menu_category.name %>
</p>

<% @menu_category.menu_items.all.each do |m| %>
	<%= m.name %> <br />
<% end %>

<%= link_to 'Edit', edit_menu_category_path(@menu_category) %> |
<%= link_to 'Back', menu_categories_path %>
